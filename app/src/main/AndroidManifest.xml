<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.popcornmovies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".mainscreen.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".detailscreen.DetailActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".mainscreen.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".mainscreen.MainActivity" />
        </activity>
        <activity
            android:name=".mainscreen.SettingsActivity"
            android:parentActivityName=".mainscreen.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".mainscreen.MainActivity" />
        </activity>
        <activity
            android:name=".reviewscreen.ReviewsActivity"
            android:parentActivityName=".detailscreen.DetailActivity">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".detailscreen.DetailActivity" />
        </activity>
    </application>

</manifest>